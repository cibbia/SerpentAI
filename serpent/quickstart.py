from __future__ import annotations

def generate_quickstart_plugin(window_title: str) -> str:
    """Stub that in future will auto-create a generic plugin.

    For now, simply return a fake plugin name so the GUI works.
    """
    return "Generic_QUICKSTART"